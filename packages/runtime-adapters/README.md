# `@foundry/runtime-adapters`

Controlled adapters that invoke external runtimes (for example Claude Code) behind backend policy.

## Status

Reserved package placeholder. No implementation in Foundation v1.0.

## Intended contents

- Adapter interfaces for `mock` and `claude_code` runtimes
- Allowlist / denylist policy types
- Structured result, log, and evidence capture contracts
- Alignment with ADR-006 and `docs/02-specification/domain-model.md` (`AgentRun`)
