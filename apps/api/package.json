{
  "name": "@foundry/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "build": "node build.mjs",
    "dev": "node build.mjs && node dist/main.js",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@foundry/contracts": "workspace:*",
    "@foundry/event-types": "workspace:*",
    "@foundry/persistence": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
